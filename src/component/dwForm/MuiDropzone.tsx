import React from "react";

const MuiDropzone = () => {
  return (
    <div>
      <Field name={props.name}>
        {({ field, meta }: FieldProps) => {
          return <div></div>;
        }}
      </Field>
    </div>
  );
};

export default MuiDropzone;
